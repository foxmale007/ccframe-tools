package <%= it.basePackage %>.<%= it.packageName %>.service;

import org.ccframe.commons.base.BusinessException;
import org.ccframe.commons.base.IBaseSearchService;
import <%= it.basePackage %>.<%= it.packageName %>.domain.entity.<%= it.entityName %>;

public interface I<%= it.entityName %>SearchService extends IBaseSearchService<<%= it.entityName %>> {

}
